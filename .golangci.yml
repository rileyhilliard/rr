version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    # Default/essential
    - govet
    - errcheck
    - staticcheck
    - unused
    - ineffassign

    # Code quality
    - gocritic
    - gocyclo
    - misspell
    - unconvert
    - unparam

    # Security
    - gosec

    # Style consistency
    - revive
    - nolintlint

  settings:
    errcheck:
      check-blank: false
      exclude-functions:
        # Common functions where ignoring errors is acceptable
        - os.Setenv
        - os.Unsetenv
        - os.RemoveAll
        - os.Chdir
        - os.Getwd
        - os.UserHomeDir
        # Print functions where errors are generally not actionable
        - fmt.Fprintf
        - fmt.Fprintln
        - fmt.Fprint
        - (io.Closer).Close

    staticcheck:
      checks:
        - all
        # QF1012 rewrites sb.WriteString(fmt.Sprintf(...)) as fmt.Fprintf(&sb,
        # ...). Same behavior, and the WriteString form reads better next to
        # the plain WriteString calls it sits among. Style preference, not a bug.
        - -QF1012

    gocyclo:
      min-complexity: 30  # CLI tools often have complex switch statements

    gocritic:
      enabled-tags:
        - diagnostic
        - performance
      disabled-checks:
        - hugeParam        # Too noisy for value receivers on small structs
        - appendCombine    # Style preference, not a bug
        - ifElseChain      # Style preference
        - wrapperFunc      # Style preference

    revive:
      rules:
        - name: blank-imports
        - name: context-as-argument
        - name: context-keys-type
        - name: dot-imports
        - name: error-return
        - name: error-strings
        - name: error-naming
        - name: exported
          disabled: true  # Too noisy for CLI tools
        - name: increment-decrement
        - name: var-naming
          disabled: true  # Intentional shadowing of stdlib errors package
        - name: var-declaration
        - name: package-comments
          disabled: true
        - name: range
        - name: receiver-naming
        - name: time-naming
        - name: unexported-return
        - name: indent-error-flow
        - name: errorf
        - name: empty-block
        - name: superfluous-else
        - name: unused-parameter
          disabled: true  # unparam handles this
        - name: unreachable-code
        - name: redefines-builtin-id

    gosec:
      excludes:
        - G104  # Audit errors not checked (errcheck handles this)
        - G204  # Subprocess with variable (expected for CLI that runs user commands)
        - G301  # Directory permissions 0755 (standard for CLI tools)
        - G304  # File path from variable (common in CLI tools)
        - G306  # WriteFile permissions 0644 (standard for config files)
        - G602  # Slice bounds (false positive in controlled code)
        # Taint-analysis rules added in gosec 2.23 (golangci-lint 2.13). They
        # are the dataflow-tracking counterparts of G204/G304 above and flag
        # the same by-design behavior: rr's whole job is to run the command
        # the user asked for, on the host and with the SSH key their config
        # names. The inputs are the operator's own config and argv, not
        # untrusted remote data.
        - G702  # Command injection (rr executes user-supplied commands)
        - G703  # Path traversal (SSH key/cert paths come from user config)
        - G704  # SSRF (SSH/agent endpoints come from user config)
        - G706  # Log injection (host names from user config reach logs)

    nolintlint:
      require-explanation: true
      require-specific: true

  exclusions:
    generated: lax
    presets:
      - comments
      - std-error-handling
    rules:
      # Allow Close() errors to be ignored in defer statements
      - linters:
          - errcheck
        source: "defer.*Close\\(\\)"
      # Allow forceRemove errors to be ignored in lock cleanup
      - linters:
          - errcheck
        source: "forceRemove\\("
      # Test files can be more relaxed
      - path: _test\.go
        linters:
          - gocyclo
          - gosec
          - errcheck
          - unparam

formatters:
  enable:
    - gofmt
    - goimports
